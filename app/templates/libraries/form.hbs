<h2>{{title}}</h2>
<div class="row">
  <div class="col-md-6">
    <LibraryItemForm @item={{model}} @buttonLabel={{buttonLabel}} @handleClick={{action "saveLibrary"}} />
  </div>
  <div class="col-md-4">
    <LibraryItem @item={{model}} @badge={{model.books.length}}>
      <br>
    </LibraryItem>
  </div>
</div>